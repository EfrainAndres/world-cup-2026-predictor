{
  "name": "@world-cup-2026-predictor/model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "calibration:baseline": "vitest run tests/elo-to-xg-calibration-artifact.test.ts",
    "calibration:v2-candidate": "vitest run tests/elo-to-xg-v2-candidate-artifact.test.ts",
    "calibration:v2-decision": "vitest run tests/elo-to-xg-v2-production-decision-artifact.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
